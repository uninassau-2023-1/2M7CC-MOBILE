import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projeto1-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
